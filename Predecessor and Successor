//It's a program that writes a program that reads an integer and prints its predecessor (the previous integer) and its successor (posterior integer).

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
	int n;
	scanf ("%d" , &n);
	int na = n-1;
	int np = n+1;
	
	printf("%d %d\n", na, np);

return 0;
}
