// Read 2 float values with double precision A and B, which corresponding to 2 grades of a student.
//After this, calculate the average grade of the student, 
//knowing that grade A has weight 3.5 
//and that grade B has weight 7.5 (the sum of the weights is 11). 
//Suppose that each grade has a range of 0 to 10.0,
//always formatted to one decimal place.

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
	double n1;
	double n2;
	
	scanf("%lf %lf", &n1, &n2);
	
	double mean = (n1*3.5+n2*7.5)/11;
	
	printf("MEAN = %.5lf\n", mean);

 return 0;
}
